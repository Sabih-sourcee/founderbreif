# Create at https://aistudio.google.com/apikey (new keys use AQ. prefix — that's normal)
GEMINI_API_KEY=your_gemini_api_key_here

# Admin account — unlimited brief generations (set in Profile → Admin Login on mobile)
ADMIN_EMAIL=bookspower218@gmail.com
ADMIN_PASSWORD=your_admin_password_here

# Used for OAuth callbacks and self-referential links (local dev: your PC LAN IP)
APP_URL=http://192.168.0.106:3000